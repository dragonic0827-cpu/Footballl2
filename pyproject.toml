[project]
name = "football-world"
version = "0.1.0"
description = "Deterministic, continuous international football history simulation core"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

